# version: '2.1'
services:
  nginx-react:
    container_name: ngixreactapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9081:80
      - 9443:443
    environment:
      NODE_ENV: production
